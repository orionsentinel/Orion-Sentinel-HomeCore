# =============================================================================
# ESPHome Stack
# =============================================================================

services:
  esphome:
    image: ghcr.io/esphome/esphome:latest
    container_name: esphome
    restart: unless-stopped
    init: true
    stop_grace_period: 10s
    environment:
      - TZ=${TZ:-Europe/Amsterdam}
      - ESPHOME_DASHBOARD_USE_PING=true
    volumes:
      - ${DATA_ROOT}/esphome/config:/config
    ports:
      - "${HOST_IP:-127.0.0.1}:${ESPHOME_PORT:-6052}:6052"
    networks:
      - homecore_internal
      - homecore_lan
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6052/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        reservations:
          memory: "128M"
    profiles:
      - esphome
